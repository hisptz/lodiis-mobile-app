import 'package:flutter/material.dart';

class OvcExitPage extends StatelessWidget {
  const OvcExitPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text('Ovc Exit page')],
      ),
    );
  }
}
