import 'package:flutter/material.dart';

class OvcEnrollmentPage extends StatelessWidget {
  const OvcEnrollmentPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text('Ovc Enrollment page')],
      ),
    );
  }
}
