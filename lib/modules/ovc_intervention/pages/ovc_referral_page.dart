import 'package:flutter/material.dart';

class OvcReferralPage extends StatelessWidget {
  const OvcReferralPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text('Ovc Referral page')],
      ),
    );
  }
}
