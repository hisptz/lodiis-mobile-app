import 'package:flutter/material.dart';

class OvcRecordsPage extends StatelessWidget {
  const OvcRecordsPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text('Ovc Records page')],
      ),
    );
  }
}
