import 'package:flutter/material.dart';

class DreamsExitPage extends StatelessWidget {
  const DreamsExitPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text('Dreams Exit page')],
      ),
    );
  }
}
