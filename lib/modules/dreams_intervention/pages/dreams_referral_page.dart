import 'package:flutter/material.dart';

class DreamsReferralPage extends StatelessWidget {
  const DreamsReferralPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text('Dreams Referral page')],
      ),
    );
  }
}
